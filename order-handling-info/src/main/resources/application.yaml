server:
  port: 8085
spring:
  application:
    name: order-handling-info
  datasource:
    password: 'password'
    url: jdbc:postgresql://localhost:5432/ethanol
    username: 'rszumlas'
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true