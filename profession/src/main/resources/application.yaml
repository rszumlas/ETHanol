server:
  port: 8087
spring:
  application:
    name: profession
  datasource:
    password: 'password'
    url: jdbc:postgresql://localhost:5432/ethanol
    username: 'rszumlas'
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true