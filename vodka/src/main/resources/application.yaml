server:
  port: 8089
spring:
  application:
    name: vodka
  datasource:
    password: password
    url: jdbc:postgresql://postgres:5432/vodka
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true