server:
  port: 8088
spring:
  application:
    name: shelf
  datasource:
    password: password
    url: jdbc:postgresql://postgres:5432/shelf
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true